# ==========================================
# MARIE Chat - Environment Configuration
# ==========================================

# ------------------------------------------
# OpenSearch Configuration (REQUIRED)
# ------------------------------------------
# External OpenSearch instance connection details
OPENSEARCH_HOSTS=http://your-opensearch-host:9200
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=your-opensearch-password
OPENSEARCH_USE_SSL=false
OPENSEARCH_VERIFY_CERTS=false

# OpenSearch Performance Tuning (Optional)
# OPENSEARCH_TIMEOUT=30
# OPENSEARCH_MAX_RETRIES=3

# ------------------------------------------
# Ollama Configuration (REQUIRED)
# ------------------------------------------
# External Ollama instance URL
# Default: http://localhost:11434
OLLAMA_BASE_URL=http://your-ollama-host:11434

# ------------------------------------------
# Application Secrets (REQUIRED)
# ------------------------------------------
JWT_SECRET_KEY=change-this-to-a-random-secret-key

# ------------------------------------------
# HuggingFace API (Optional)
# ------------------------------------------
# Required for image generation and some embedding models
HUGGINGFACE_API_KEY=

# HuggingFace Models Configuration
# NOTE: Models are downloaded automatically on first use (lazy loading)
# You can pre-download models by running: python backend/download_models.py

# Enable automatic model download on startup (optional)
# Set to "true" to download all models when the backend starts
# HF_AUTO_DOWNLOAD=false

# Comma-separated list of models to pre-download (if HF_AUTO_DOWNLOAD=true)
# Leave empty to download only the models configured below

# Text Embeddings (for semantic search and RAG)
# Downloaded automatically when embedding features are used
HF_EMBEDDING_MODELS=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2

# Image Generation Models
# Downloaded automatically when image generation is requested
HF_IMAGE_MODELS=stabilityai/stable-diffusion-3.5-large,black-forest-labs/FLUX.1-dev

# Speech-to-Text Models (Whisper variants)
# Downloaded automatically when STT is used
# Options: tiny, base, small, medium, large-v1, large-v2, large-v3
HF_STT_MODELS=Systran/faster-whisper-base,Systran/faster-whisper-small

# Text-to-Speech Models
# HF_TTS_MODELS=

# Complete list (all models) - uncomment to download all at once
# HF_MODELS=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2,stabilityai/stable-diffusion-3.5-large,black-forest-labs/FLUX.1-dev,Systran/faster-whisper-base

# ------------------------------------------
# OpenAI Compatible API (Optional)
# ------------------------------------------
# For external OpenAI-compatible LLM providers
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_DEFAULT_MODEL=gpt-4o

# ------------------------------------------
# Embedding Configuration
# ------------------------------------------
# Model used for text embeddings (semantic search, RAG, memory)
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
EMBEDDING_DIMENSION=384

# ------------------------------------------
# Speech-to-Text Configuration
# ------------------------------------------
# Whisper model size: tiny, base, small, medium, large-v1, large-v2, large-v3
WHISPER_MODEL=base
WHISPER_DEVICE=auto

# ------------------------------------------
# Image Generation Configuration
# ------------------------------------------
# Default model for local image generation
IMAGE_DEFAULT_MODEL=stabilityai/stable-diffusion-3.5-large

# ------------------------------------------
# Docker Compose Internal Services
# ------------------------------------------
# These values are set automatically when services run in docker-compose
# You only need to override these if running services externally
# FLASK_ENV=development
# PORT=5000

# ------------------------------------------
# GPU Configuration (Optional)
# ------------------------------------------
# NVIDIA GPU settings for PyTorch
# NVIDIA_VISIBLE_DEVICES=all
# NVIDIA_DRIVER_CAPABILITIES=compute,utility
# PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
